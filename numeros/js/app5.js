//existen incrementadores y decrementadores

//incluir incrementos y decrementso de 1 en 1 u cualquier valor.

let conteo = 7;
console.log(conteo);
conteo++;//es lo mismo conteo = conteo+1;
console.log(conteo);
conteo--;
console.log(conteo);

//++conteo
//--conteo

conteo +=7;// es lo mismo conteo = conteo + 7
console.log(conteo);
conteo +=7;// es lo mismo conteo = conteo + 7
console.log(conteo);
conteo +=7;// es lo mismo conteo = conteo + 7
console.log(conteo);
conteo +=7;// es lo mismo conteo = conteo + 7
console.log(conteo);
conteo +=7;// es lo mismo conteo = conteo + 7
console.log(conteo);

conteo -=5;
console.log(conteo)